" setting 'exrc' and 'secure' in your ~/.vimrc will allow vim to read this file

" size of a tab character
set tabstop=4

set expandtab

" size of an "indent"
set shiftwidth=4

" how many columns to use when hitting tab
set softtabstop=4

